# man

Inspired (stolen) from [manpj](https://github.com/pjungwir/manpj/).
Clone this repository into a directory, run `make`, add the directory `manedd` to your `MANPATH`, and `edd` to `MANSECT`.
Now, you can access the manpages via `man edd <page>`.
